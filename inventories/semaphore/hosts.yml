---
all:
  hosts:
    localhost:
      ansible_connection: local
  children:
    proxmox_api:
      hosts:
        proxmox-node:
          ansible_host: 192.168.123.51
          ansible_user: root
    semaphore_ui:
      hosts:
        semaphore-server:
          ansible_host: 192.168.123.50
          ansible_user: root
